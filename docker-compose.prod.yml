services:
  google-contacts:
    build: .
    container_name: google-contacts
    restart: unless-stopped
    expose:
      - "8080"
    env_file:
      - /app/data/google-contacts/.env
    networks:
      - proxy-network

networks:
  proxy-network:
    external: true
